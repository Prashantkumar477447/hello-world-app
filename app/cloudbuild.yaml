steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/hello-world-app', '.']

images:
  - 'gcr.io/$PROJECT_ID/hello-world-app'

options:
  logging: CLOUD_LOGGING_ONLY
